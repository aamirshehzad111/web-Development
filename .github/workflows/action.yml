name: Feature Merge Workflow

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: exit 1 steps
        run: ls /aaaaaaa/aaaaaaa

      - name: Additional steps
        run: echo "test"
